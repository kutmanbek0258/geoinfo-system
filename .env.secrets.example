# This file contains sensitive environment variables for the application.
# It should NOT be committed to version control with actual values.
# Rename this file to .env.secrets (or .env for local development) and fill in the values.
#
# To use this file with docker-compose, explicitly specify it during the docker-compose up command:
# docker-compose --env-file .env.secrets --env-file .env.example up
#
# Note: For production, it's highly recommended to provide these variables through a secure mechanism
# (e.g., Kubernetes secrets, Vault, or environment variables directly on the host).

#================================================================================
# Учетные данные и секреты баз данных (Database Credentials & Secrets)
#================================================================================
# --- PostgreSQL для auth-service ---
POSTGRES_USER_AUTH=postgres
POSTGRES_PASSWORD_AUTH=password

# --- PostgreSQL для geodata-service ---
POSTGRES_USER_GEODATA=postgres
POSTGRES_PASSWORD_GEODATA=password

# --- PostgreSQL для document-service ---
POSTGRES_USER_DOCS=postgres
POSTGRES_PASSWORD_DOCS=password

# --- Redis для auth-service ---
REDIS_PASSWORD=qwerty12345678

#================================================================================
# Учетные данные и секреты для межсервисного взаимодействия (Inter-Service)
#================================================================================
# --- Клиент для внутренней проверки токенов (introspection) ---
# Используется микросервисами для обращения к auth-service для валидации токенов.
J_SERVICE_CLIENT_ID=j-service-client
J_SERVICE_CLIENT_SECRET=j-service-client

# --- Внутренний клиент SSO в auth-service ---
# Секрет клиента для самогО auth-service при его работе с OAuth2.
SSO_CLIENT_SECRET=sso-client

#================================================================================
# Учетные данные и секреты внешних OAuth2 провайдеров (External OAuth2)
#================================================================================
# --- SMTP для отправки почты (Mail Service) ---
SMTP_USER=your_smtp_user@example.com
SMTP_PASSWORD=your_smtp_password

# --- GitHub OAuth App ---
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- Google OAuth App ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Yandex OAuth App ---
YANDEX_CLIENT_ID=
YANDEX_CLIENT_SECRET=

# --- MS Azure OAuth App ---
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

#================================================================================
# Учетные данные и секреты инфраструктурных сервисов (Infrastructure Services)
#================================================================================
# --- MinIO S3 Storage ---
MINIO_ROOT_USER=minio_access_key
MINIO_ROOT_PASSWORD=minio_secret_key

# --- GeoServer ---
GEOSERVER_ADMIN_USER=admin
GEOSERVER_ADMIN_PASSWORD=geoserver

# --- OnlyOffice Document Server ---
ONLYOFFICE_JWT_SECRET=a_very_long_and_secure_secret_key_for_onlyoffice_jwt_32_chars