spring:
  application:
    name: search-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true

  eureka:
    client:
      service-url:
        defaultZone: http://discovery-server:8761/eureka/