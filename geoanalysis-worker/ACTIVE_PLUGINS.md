# Реестр активных плагинов GeoAnalysis Worker

В данном документе перечислены все функциональные модули (плагины), доступные в текущей сборке воркера, с описанием их входных данных и параметров.

---

## 1. Terrain Contours (Генерация изолиний)
**Идентификатор (`pluginName`):** `terrain_contours`

Плагин для извлечения векторных линий равных высот из цифровых моделей рельефа (DEM).

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр высот | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `interval` | Шаг между изолиниями в метрах | float | `10.0` |
| `base` | Базовая высота для начала отсчета. Если не задан или пуст, автоматически определяется минимальная отметка высоты DEM (самая низкая точка) | float | Динамическое (DEM Min) |
| `attribute` | Имя атрибута высоты в итоговом файле | string | `"elev"` |
| `use_3d` | Создавать ли 3D геометрию (Z-координату) | boolean | `false` |
| `format` | Формат выходного файла | string | `"GeoJSON"` |

### Результаты (`outputs`)
*   `vector_result`: S3 URL к созданному файлу изолиний.

---

## 2. Zonal Statistics (Зональная статистика)
**Идентификатор (`pluginName`):** `zonal_statistics`

Плагин для расчета статистических показателей растра в границах векторных зон.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `source_raster` | Растр со значениями для анализа | S3 URL (GeoTIFF) |
| `zones_vector` | Векторный слой с полигонами зон | S3 URL (GeoJSON) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `stats` | Список метрик (`min`, `max`, `mean`, `count`, `sum`, `std`, `median`) | array | `["mean", "count"]` |
| `categorical` | Считать ли количество каждого уникального значения | boolean | `false` |
| `add_properties` | Сохранять ли атрибуты исходного вектора | boolean | `true` |

### Результаты (`outputs`)
*   `statistics_json`: S3 URL к JSON-файлу с массивом статистических данных.
*   `statistics_geojson`: S3 URL к GeoJSON-файлу с геометрией и статистикой в атрибутах.

---

## 3. Clip Raster by Mask (Обрезка растра)
**Идентификатор (`pluginName`):** `clip_raster_by_mask`

Плагин для обрезки растрового слоя по границам векторного мультиполигона.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `source_raster` | Исходный растр (DEM или снимок) | S3 URL (GeoTIFF) |
| `mask_vector` | Векторная маска (полигоны) | S3 URL (GeoJSON) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `nodata_value` | Значение для пустых пикселей вне маски | float | `0.0` |
| `crop_to_cutline` | Обрезать ли растр до охвата маски | boolean | `true` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новому обрезанному COG растру.

---

## 4. Slope (Крутизна склонов)
**Идентификатор (`pluginName`):** `slope`

Плагин для расчета углов наклона поверхности по цифровой модели рельефа (DEM). Автоматически преобразует единицы измерения (градусы в метры) при использовании географической системы координат.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр высот | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `units` | Формат вывода: в градусах (`degrees`) или процентах (`percent`) | string | `"degrees"` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новому растру крутизны склонов (COG).

---

## 5. Aspect (Экспозиция склонов)
**Идентификатор (`pluginName`):** `aspect`

Плагин для расчета направления склонов (экспозиции) по цифровой модели рельефа (DEM). Направление измеряется в градусах от 0 (север) до 360 (по часовой стрелке).

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр высот | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
*   Параметры отсутствуют.

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новым растрам экспозиции склонов (COG).

---

## 6. Hillshade (Теневая отмывка)
**Идентификатор (`pluginName`):** `hillshade`

Плагин для моделирования освещенности рельефа солнцем с целью создания наглядного 2D-представления рельефа.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр высот | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `azimuth` | Угол азимута источника света (0-360) | float | `315.0` |
| `altitude` | Высота источника света над горизонтом (0-90) | float | `45.0` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новому растру отмывки рельефа (COG).

---

## 7. Viewshed Analysis (Анализ зон видимости)
**Идентификатор (`pluginName`):** `viewshed_analysis`

Плагин для расчета прямой видимости из заданной точки наблюдения с учетом рельефа местности.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр высот | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `observer_x` | Долгота (X) точки наблюдения | float | Обязательный |
| `observer_y` | Широта (Y) точки наблюдения | float | Обязательный |
| `observer_height` | Высота наблюдателя над уровнем земли (в метрах) | float | `2.0` |
| `target_height` | Высота цели над уровнем земли (в метрах) | float | `0.0` |
| `max_distance` | Максимальный радиус анализа (в метрах) | float | `10000.0` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новому бинарному растру зон видимости (1 - видно, 0 - не видно) (COG).

---

## 8. Spectral Indices (Спектральные индексы)
**Идентификатор (`pluginName`):** `spectral_indices`

Плагин для автоматического расчета вегетационных, гидрологических и других индексов по каналам мультиспектральных снимков.

### Входные данные (`inputs`)
В зависимости от рассчитываемого индекса, в `inputs` должны быть переданы соответствующие каналы:
*   `red`: Красный канал (для NDVI)
*   `green`: Зеленый канал (для NDWI)
*   `nir`: Ближний инфракрасный (NIR) канал (для NDVI, NDWI, NBR, NDRE)
*   `swir`: Коротковолновый инфракрасный (SWIR) канал (для NBR)
*   `re`: Канал Red Edge (для NDRE)

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `index_type` | Тип индекса: `NDVI`, `NDWI`, `NBR`, `NDRE` | string | `"NDVI"` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к результирующему растру индекса (COG).

---

## 9. Raster Reclassification (Реклассификация растра)
**Идентификатор (`pluginName`):** `raster_reclass`

Плагин для изменения пиксельных значений растрового слоя на основе заданных числовых интервалов.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `source_raster` | Исходный растр | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `rules` | Массив правил переклассификации в формате `[[min, max, new_val], ...]` | array/JSON | Обязательный |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к новому реклассифицированному растру (COG).

---

## 10. Raster Algebra (Алгебра растров)
**Идентификатор (`pluginName`):** `raster_algebra`

Математический калькулятор для проведения попиксельных операций над несколькими растрами.

### Входные данные (`inputs`)
Входные данные передаются динамически. Ключи словаря соответствуют переменным, используемым в выражении (например: `A`, `B`).

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `expression` | Математическое выражение для вычисления (например: `"(A - B) / (A + B + 0.001)"`) | string | Обязательный |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к результирующему растру (COG).

---

## 11. Rasterize Vector (Растеризация вектора)
**Идентификатор (`pluginName`):** `rasterize_vector`

Плагин для преобразования векторных объектов (полигонов/линий) в растровый формат с использованием заданного шаблона.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `vector_features` | Слой векторных объектов | S3 URL (GeoJSON) |
| `template_raster` | Растр-шаблон для извлечения пространственного разрешения и привязки | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `attribute_field` | Имя поля вектора для "выжигания" его значений в пиксели | string | `null` (используется `default_value`) |
| `default_value` | Значение по умолчанию для записи в пиксели (если поле не задано) | float | `1.0` |
| `nodata_value` | Значение для пустых зон (вне геометрии векторов) | float | `0.0` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к созданному растру (COG).

---

## 12. Polygonize Raster (Векторизация растра)
**Идентификатор (`pluginName`):** `polygonize_raster`

Преобразование пиксельных зон классифицированного растра в векторные полигоны.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `classified_raster` | Исходный целочисленный растр | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `connectivity` | Тип связности пикселей (`4` или `8` направлений) | integer | `4` |
| `mask_zero` | Исключать ли нулевые значения из результирующего вектора | boolean | `true` |
| `format` | Выходной векторный формат (`GeoJSON` или `GPKG`) | string | `"GeoJSON"` |

### Результаты (`outputs`)
*   `vector_result`: S3 URL к файлу векторизованных полигонов.

---

## 13. Raster Mosaic (Мозаика растров)
**Идентификатор (`pluginName`):** `raster_mosaic`

Плагин для объединения (сшивки) нескольких смежных растровых фрагментов (тайлов) в единое изображение.

### Входные данные (`inputs`)
Набор растров передается динамически в виде словаря путей к файлам S3.

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `resampling` | Алгоритм передискретизации: `nearest`, `bilinear`, `cubic`, `cubicspline`, `lanczos`, `average`, `mode` | string | `"nearest"` |
| `nodata` | Значение для пустых пикселей (NoData) | float | `null` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к объединенной мозаике (COG).

---

## 14. Unsupervised Classification (Неконтролируемая классификация)
**Идентификатор (`pluginName`):** `unsupervised_class`

Спектральное кластеризованное сегментирование растра на заданное число классов по алгоритму K-Means.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `multiband_raster` | Мультиспектральный снимок | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `clusters_count` | Количество результирующих классов (кластеров) | integer | `5` |
| `max_iter` | Максимальное количество итераций схождения K-Means | integer | `100` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к классифицированному одноканальному растру классов (COG).

---

## 15. Watershed Delineation (Выделение водосборов)
**Идентификатор (`pluginName`):** `watershed_delineation`

Гидрологический анализ DEM для автоматического расчета направления стоков, построения речной сети и выделения водосборного бассейна.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_raster` | Исходный растр высот (DEM) | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `target_point_x` | Долгота (X) устьевой точки водосбора (замыкающего створа) | float | Обязательный |
| `target_point_y` | Широта (Y) устьевой точки водосбора (замыкающего створа) | float | Обязательный |
| `threshold` | Порог накопления стока для определения русел (аккумуляция) | integer | `1000` |

### Результаты (`outputs`)
*   `basin_vector`: S3 URL к полигону бассейна (GeoJSON).
*   `streams_raster`: S3 URL к растру выделенной речной сети (GeoTIFF).

---

## 16. Viewshed Analysis Dynamic (Динамический анализ зон видимости)
**Идентификатор (`pluginName`):** `viewshed_analysis_dynamic`
**Тип:** Устанавливаемый (Schema-driven UI)

Динамическая версия анализа зон видимости с поддержкой координат, передаваемых в виде вложенного объекта-точки.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `dem_file` | Исходный растр рельефа высот (DEM) | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `observer_point` | Точка наблюдателя (объект с полями `x` и `y`) | object | Обязательный |
| `observer_height` | Высота наблюдателя (м) | float | `2.0` |
| `target_height` | Высота цели (м) | float | `0.0` |
| `max_distance` | Максимальная дистанция видимости (м) | float | `10000.0` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к растру зон видимости (COG).

---

## 17. Raster Reclass Dynamic (Динамическая реклассификация растра)
**Идентификатор (`pluginName`):** `raster_reclass_dynamic`
**Тип:** Устанавливаемый (Schema-driven UI)

Динамическая реклассификация растровых пикселей по диапазонам.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| `source_raster` | Исходный растровый слой | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `default_value` | Значение по умолчанию для пикселей вне интервалов | float | `0` |
| `rules` | Матрица правил интервалов (массив `[ [min, max, new_val], ... ]`) | array | `[[0, 100, 1]]` |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к реклассифицированному растру (COG).

---

## 18. Raster Algebra Dynamic (Динамическая алгебра растров)
**Идентификатор (`pluginName`):** `raster_algebra_dynamic`
**Тип:** Устанавливаемый (Schema-driven UI)

Математический калькулятор растров с динамическим набором переменных.

### Входные данные (`inputs`)
| Ключ | Описание | Формат |
| --- | --- | --- |
| Динамический (например, `A`, `B`, `C`) | Исходные растры-переменные | S3 URL (GeoTIFF) |

### Параметры (`parameters`)
| Параметр | Описание | Тип | По умолчанию |
| --- | --- | --- | --- |
| `expression` | Математическое выражение (формула) | string | Обязательный |

### Результаты (`outputs`)
*   `raster_result`: S3 URL к результату вычисления растровой формулы (COG).

---

## Общие примечания
*   **Идемпотентность:** Все плагины перезаписывают результаты при повторном запуске с тем же `taskId`.
*   **Очистка:** Все промежуточные данные удаляются из RAM-диска сразу после загрузки результатов в S3.
*   **Проекции:** Плагины автоматически выполняют перепроецирование (Reproject) векторных данных к системе координат растра для обеспечения точности расчетов.
